@model IList<QuestionViewModel>

<p>Решай тест давай</p>
<div>
    <form enctype="multipart/form-data" asp-controller="Result" asp-action="Index" method="post">
        @for (int i = 0; i < Model.Count; i++)
        {
            <div class="question">
                <div class="question-image">
                    <img width="400" src="data:image/png;base64, @Model[i].ImageBase64" alt="Картинка" />
                </div>
                <div class="select-answer">
                    @foreach (var answer in Model[i].Options)
                    {
                        <label>
                            <input name="[@i].SelectedAnswer" type="radio" value="@answer" />
                            @answer
                        </label> <br />
                    }
                </div>
            </div>
            <input type="hidden" name="[@i].CorrectAnswer" value="@Model[i].CorrectAnswer"/>
        }
        <button>Отправить</button>
    </form>
</div>